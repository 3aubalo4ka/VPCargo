<% if (flashError && flashError.length) { %>
  <p class="alert error"><%= flashError[0] %></p>
<% } %>
<% if (flashSuccess && flashSuccess.length) { %>
  <p class="alert success"><%= flashSuccess[0] %></p>
<% } %>
